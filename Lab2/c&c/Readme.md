# Laboratory #2
Juan Felipe Hernández Ochoa

## Lab development
In this Readme you can find the test deploy of code, an image describing the components and connectors system view of proyect and descriptions of components and connectors.

The system create a database modification with a HTTP request. Exist a Mongo database and a SQL database with an API gateway for each one.

In code folder, you can find a folder to component 2 and a folder for componet 4 (Code for Api's). The other componets are create with the docker compose because are the Mongo and SQL databases. Those database are create in virtual docker containers.

The following images show the Flow test for two request of each database.

__FLOW 1__

![If you can't see the image, you can find in the folder 'images' with name Flow1](/images/Flow1.jpg)

__FLOW 2__

![If you can't see the image, you can find in the folder 'images' with name Flow2](/images/Flow2.jpg)



## Components and connectors view

![If you can't see the image, you can find in the folder 'images' with name c&c_view](/images/c&c_view.jpg)

## Descriptions of components and connectors

![If you can't see the image, you can find in the folder 'images' with name c&c_view](/images/c&c_description.jpg)